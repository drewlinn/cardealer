<ul>
  @foreach (var car in Model)
  {
    <li>@car.GetMakeModel()</li>
    <li>@car.GetPrice()</li>
    <li>@car.GetMiles()</li>
  }
</ul>

<button><a href="/">Add a new car</a></button>

<button><a href="/cars_cleared">Clear All Cars</a></button>
